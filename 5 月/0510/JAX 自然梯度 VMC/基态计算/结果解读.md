我目前已经成功地实现了基态计算，得到了收敛的model参数。
model 的输出为 $\ln{\psi(s)}$  
接下来我想要得到基态能量 $E_{gs}$。 
在我的理解中: 目前的波函数为 
$$
\psi(x) = p_0 * \psi(x_0) + p_1 * \psi(x_1) + p_2 * \psi(x_2) + p_3 * \psi(x_3)
$$
$p_0$为 Born 分布概率
因此,要计算基态能量E_{gs}, 我需要计算 
$$
E_{gs} = \langle \psi(x) | H | \psi(x) \rangle
$$