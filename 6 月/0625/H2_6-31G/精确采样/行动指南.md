对，我给你一版**可以直接粘进 notebook 的 K=3 枚举版 NES-VMC 代码**。它的目的不是最终替代采样器，而是先把 sampler 完全拿掉，检查：

```text
NES determinant 本体
E_L matrix
gradient
optimizer
K=3 子空间分离
```

到底能不能活。

你现在原来的训练循环是从 `nes_sampler.sample(...)` 得到 `x_batch`，然后喂给 `nes_vmc_gradient_stable(...)`。你已有的稳定版设计本来就是通过 `total_matrix_machine / total_max_machine / Ham_Psi_scaled` 来稳定计算 (E_L=\Psi^{-1}H\Psi)，不是直接 raw `exp(logψ)`，这点很关键。 你之前的训练循环也是把采样得到的 `x_batch` 传进 `nes_vmc_gradient_stable(...)`，所以枚举版的核心就是：**把 sampler 产生的 `x_batch` 换成枚举出的 `valid_X`**。

下面这版我写成“独立诊断训练 cell”。你先跑这个，不要再混 MetropolisSampler，不然嫌疑人又多一个，代码就开始演悬疑片。

---

# 0. 前提

假设你前面已经定义好了这些对象：

```python
K = 3
SINGLE_SIZE = hi.size  # H2 / 6-31G 应该是 8

ha
hi
E_fcis

total_machine
total_matrix_machine
total_max_machine
single_machine_list
total_params

Ham_Psi_scaled
```

如果你使用了 gauge-fixed wrapper，也可以直接用这套代码。你的 wrapper 接法里 `total_machine / total_matrix_machine / total_max_machine / single_machine_list` 都已经统一 gauge-fixed，这正是应该喂给 loss 的接口。

---

# 1. 枚举所有合法 NES 构型

K=3 时，单系统 16 个 determinant，合法 ordered NES samples 数量：

[
16\times 15\times 14=3360
]

```python
import itertools
import time
import numpy as np

import jax
import jax.numpy as jnp
import optax
from jax.flatten_util import ravel_pytree


def build_valid_nes_configurations(hi, K, single_size, dtype=jnp.float32):
    """
    枚举所有合法 NES samples:
        X = [x_1, ..., x_K]
    要求:
        x_i != x_j  for i != j

    返回:
        valid_X: shape (N_valid, K, single_size)
        perm_indices: shape (N_valid, K)
        single_states: shape (N_single, single_size)
    """
    single_states = jnp.asarray(hi.all_states(), dtype=dtype)
    n_single = single_states.shape[0]

    perm_indices_np = np.asarray(
        list(itertools.permutations(range(n_single), K)),
        dtype=np.int32,
    )

    perm_indices = jnp.asarray(perm_indices_np)
    valid_X = single_states[perm_indices]

    assert valid_X.shape[1:] == (K, single_size), (
        f"valid_X.shape={valid_X.shape}, expected (-1, {K}, {single_size})"
    )

    print("=" * 80)
    print("NES full enumeration")
    print("=" * 80)
    print(f"single Hilbert size = {n_single}")
    print(f"K = {K}")
    print(f"valid_X.shape = {valid_X.shape}")
    print(f"expected valid count = {np.prod([n_single - i for i in range(K)])}")
    print("=" * 80)

    return valid_X, perm_indices, single_states


valid_X, perm_indices, single_states = build_valid_nes_configurations(
    hi=hi,
    K=K,
    single_size=SINGLE_SIZE,
)
```

你应该看到：

```text
single Hilbert size = 16
K = 3
valid_X.shape = (3360, 3, 8)
expected valid count = 3360
```

如果不是这个，先别训练。又是 shape 问题的话，人类文明可以先暂停一下。

---

# 2. 稳定计算每个枚举样本的 (E_L(X))

这个函数直接复用你的 stable 路线：

[
L^{stable}=L-c
]

[
\Psi^{stable}=\exp(L^{stable})
]

[
(H\Psi)^{stable}_{ij}
=====================

e^{-c}H\psi_j(x_i)
]

[
E_L=(\Psi^{stable})^{-1}(H\Psi)^{stable}
]

这个和你稳定性策略里写的是一致的：稳定化不会改变局域能量矩阵，只是在同一尺度下求解，避免 raw exponential 爆炸。

```python
def nes_local_energy_batch_stable_enum(
    ha,
    total_matrix_machine,
    total_max_machine,
    single_machine_list,
    total_params,
    x_batch,
    cond_threshold=1e12,
):
    """
    对一批 NES samples 精确计算 local energy matrix.

    输入:
        x_batch: shape (B, K, SINGLE_SIZE)

    返回:
        loss_batch: shape (B,)
            real(trace(E_L(X)))

        E_L_batch: shape (B, K, K)

        aux:
            L_stable
            shift
            Psi_stable
            HPsi_stable
            cond_Psi
            valid
    """
    L_stable = total_matrix_machine(total_params, x_batch)
    shift = total_max_machine(total_params, x_batch)

    Psi_stable = jnp.exp(L_stable)

    HPsi_stable = Ham_Psi_scaled(
        ha=ha,
        single_machine_list=single_machine_list,
        total_params=total_params,
        x=x_batch,
        shift=shift,
    )

    def solve_one(Psi, HPsi):
        return jnp.linalg.solve(Psi, HPsi)

    E_L_batch = jax.vmap(solve_one)(Psi_stable, HPsi_stable)

    trace_batch = jnp.trace(E_L_batch, axis1=-2, axis2=-1)
    loss_batch = jnp.real(trace_batch)

    cond_Psi = jax.vmap(jnp.linalg.cond)(Psi_stable)

    valid = (
        jnp.all(jnp.isfinite(L_stable), axis=(-2, -1))
        & jnp.all(jnp.isfinite(Psi_stable), axis=(-2, -1))
        & jnp.all(jnp.isfinite(HPsi_stable), axis=(-2, -1))
        & jnp.all(jnp.isfinite(E_L_batch), axis=(-2, -1))
        & jnp.isfinite(loss_batch)
        & jnp.isfinite(cond_Psi)
        & (cond_Psi < cond_threshold)
    )

    aux = {
        "L_stable": L_stable,
        "shift": shift,
        "Psi_stable": Psi_stable,
        "HPsi_stable": HPsi_stable,
        "E_L_batch": E_L_batch,
        "trace_batch": trace_batch,
        "loss_batch": loss_batch,
        "cond_Psi": cond_Psi,
        "valid": valid,
    }

    return loss_batch, E_L_batch, aux
```

---

# 3. 计算 exact NES 权重

这里没有 sampler。我们对所有合法 (X) 计算：

[
p_\theta(X)=
\frac{|\Psi_\theta(X)|^2}
{\sum_X |\Psi_\theta(X)|^2}
]

```python
def exact_nes_weights(total_machine, total_params, x_all):
    """
    对所有合法 NES samples 计算 normalized |Psi|^2 权重.

    返回:
        weights: shape (B,)
        logPsi: shape (B,)
        logw: shape (B,)
    """
    logPsi = total_machine(total_params, x_all)

    logw = 2.0 * jnp.real(logPsi)
    finite = jnp.isfinite(logw)

    # 防止 -inf / nan 进入 logsumexp
    logw_safe = jnp.where(finite, logw, -jnp.inf)

    logZ = jax.nn.logsumexp(logw_safe)
    weights = jnp.exp(logw_safe - logZ)

    # 保险
    weights = jnp.where(jnp.isfinite(weights), weights, 0.0)
    weights = weights / (jnp.sum(weights) + 1e-300)

    return weights, logPsi, logw
```

---

# 4. exact weighted NES loss

这个函数返回：

[
L_{\text{exact}}
================

\sum_X p_\theta(X)\operatorname{Re}\operatorname{Tr}E_L(X)
]

并计算 weighted mean 的 (E_L)：

[
\bar E_L
========

\sum_X p_\theta(X)E_L(X)
]

```python
def exact_nes_loss_and_aux(
    total_params,
    x_all,
    ha,
    total_machine,
    total_matrix_machine,
    total_max_machine,
    single_machine_list,
):
    """
    exact enumeration 版本的 NES loss.

    注意:
        这里不是 sample mean。
        这里是对所有合法 X 的 |Psi(X)|^2 加权平均。
    """
    weights, logPsi, logw = exact_nes_weights(
        total_machine=total_machine,
        total_params=total_params,
        x_all=x_all,
    )

    loss_batch, E_L_batch, aux = nes_local_energy_batch_stable_enum(
        ha=ha,
        total_matrix_machine=total_matrix_machine,
        total_max_machine=total_max_machine,
        single_machine_list=single_machine_list,
        total_params=total_params,
        x_batch=x_all,
    )

    valid = aux["valid"] & jnp.isfinite(weights)

    weights_valid = jnp.where(valid, weights, 0.0)
    weights_valid = weights_valid / (jnp.sum(weights_valid) + 1e-300)

    loss_batch_safe = jnp.where(valid, loss_batch, 0.0)

    loss_exact = jnp.sum(weights_valid * loss_batch_safe)

    E_L_safe = jnp.where(
        jnp.isfinite(E_L_batch),
        E_L_batch,
        0.0 + 0.0j,
    )

    E_L_mean = jnp.sum(
        weights_valid[:, None, None] * E_L_safe,
        axis=0,
    )

    prob_entropy = -jnp.sum(
        jnp.where(weights_valid > 0, weights_valid * jnp.log(weights_valid + 1e-300), 0.0)
    )
    eff_sample_size = 1.0 / (jnp.sum(weights_valid ** 2) + 1e-300)

    out = {
        "loss_exact": loss_exact,
        "loss_batch": loss_batch,
        "E_L_batch": E_L_batch,
        "E_L_mean": E_L_mean,
        "weights": weights_valid,
        "logPsi": logPsi,
        "logw": logw,
        "valid": valid,
        "valid_ratio": jnp.mean(valid.astype(jnp.float32)),
        "cond_Psi": aux["cond_Psi"],
        "cond_mean": jnp.mean(jnp.where(valid, aux["cond_Psi"], 0.0)),
        "cond_max": jnp.max(jnp.where(valid, aux["cond_Psi"], 0.0)),
        "prob_entropy": prob_entropy,
        "eff_sample_size": eff_sample_size,
        "shift": aux["shift"],
    }

    return loss_exact, out
```

---

# 5. exact covariance 梯度

这里我不给你用 `jax.value_and_grad(exact_loss)`，因为你现在的 NES-VMC 梯度本来就是 VMC covariance 形式：

[
g\sim
\left\langle
(\operatorname{Tr}E_L-\langle\operatorname{Tr}E_L\rangle)
\overline{\nabla\log\Psi}
\right\rangle
]

你之前稳定性说明里也是这个结构。
所以枚举版就把 sample mean 改成 exact weighted mean：

[
g_{\text{exact}}
================

\sum_X
p_\theta(X)
\left(
\operatorname{Tr}E_L(X)
-----------------------

\langle \operatorname{Tr}E_L\rangle
\right)
\overline{\nabla\log\Psi(X)}
]

```python
def exact_nes_gradient_covariance(
    total_params,
    x_all,
    ha,
    total_machine,
    total_matrix_machine,
    total_max_machine,
    single_machine_list,
):
    """
    exact enumeration 版本的 NES-VMC covariance gradient.

    这不是 Monte Carlo sample mean。
    这里对所有合法 NES samples 做 |Psi|^2 加权求和。
    """
    loss_exact, aux = exact_nes_loss_and_aux(
        total_params=total_params,
        x_all=x_all,
        ha=ha,
        total_machine=total_machine,
        total_matrix_machine=total_matrix_machine,
        total_max_machine=total_max_machine,
        single_machine_list=single_machine_list,
    )

    weights = jax.lax.stop_gradient(aux["weights"])
    valid = jax.lax.stop_gradient(aux["valid"])

    E_L_batch = aux["E_L_batch"]
    E_L_mean = aux["E_L_mean"]

    E_L_safe = jnp.where(
        jnp.isfinite(E_L_batch),
        E_L_batch,
        0.0 + 0.0j,
    )

    E_L_centered = E_L_safe - E_L_mean[None, :, :]

    trace_centered = jnp.trace(E_L_centered, axis1=-2, axis2=-1)
    trace_centered = jnp.where(valid, trace_centered, 0.0 + 0.0j)
    trace_centered = jax.lax.stop_gradient(trace_centered)

    grad_logPsi = jax.grad(
        total_machine,
        argnums=0,
        holomorphic=True,
    )

    dlog_tree = jax.vmap(
        grad_logPsi,
        in_axes=(None, 0),
    )(total_params, x_all)

    def combine_one_param(g_batch):
        """
        g_batch shape:
            (B, *param_shape)
        """
        expand_shape = (weights.shape[0],) + (1,) * (g_batch.ndim - 1)

        w = weights.reshape(expand_shape)
        tc = trace_centered.reshape(expand_shape)

        valid_f = valid.astype(g_batch.real.dtype).reshape(expand_shape)

        g_safe = jnp.where(
            jnp.isfinite(g_batch),
            g_batch,
            0.0 + 0.0j,
        )

        # 保持和你原 VMC covariance 代码同一约定：conj(dlogPsi)
        weighted = w * tc * jnp.conj(g_safe) * valid_f

        return jnp.sum(weighted, axis=0)

    grad_tree = jax.tree_util.tree_map(combine_one_param, dlog_tree)

    grad_flat, _ = ravel_pytree(grad_tree)
    grad_norm = jnp.linalg.norm(grad_flat)

    aux["grad_norm"] = grad_norm
    aux["grad_flat"] = grad_flat

    return grad_tree, loss_exact, aux["E_L_mean"], aux
```

---

# 6. 诊断函数

这个函数打印 exact enumeration 下的状态。

```python
def exact_enum_diagnostics(aux, E_fcis=None, K=None):
    E_L_mean = aux["E_L_mean"]

    E_L_herm = 0.5 * (E_L_mean + E_L_mean.conj().T)

    eig_herm = jnp.linalg.eigvalsh(E_L_herm)

    eig_raw = jnp.linalg.eigvals(E_L_mean)
    eig_raw = eig_raw[jnp.argsort(jnp.real(eig_raw))]

    herm_error = (
        jnp.linalg.norm(E_L_mean - E_L_mean.conj().T)
        / (jnp.linalg.norm(E_L_mean) + 1e-12)
    )

    imag_norm = jnp.linalg.norm(jnp.imag(E_L_mean))

    loss_exact = aux["loss_exact"]
    grad_norm = aux["grad_norm"]
    valid_ratio = aux["valid_ratio"]
    cond_mean = aux["cond_mean"]
    cond_max = aux["cond_max"]
    eff_n = aux["eff_sample_size"]
    entropy = aux["prob_entropy"]

    print("-" * 90)
    print(f"Loss exact        = {float(loss_exact): .10f}")

    if E_fcis is not None and K is not None:
        target = float(np.sum(np.asarray(E_fcis[:K])))
        print(f"Target sum FCI    = {target: .10f}")
        print(f"Gap               = {float(loss_exact) - target:+.10f}")

    print(f"Grad norm         = {float(grad_norm):.6e}")
    print(f"valid_ratio       = {float(valid_ratio):.6f}")
    print(f"cond(Psi) mean    = {float(cond_mean):.6e}")
    print(f"cond(Psi) max     = {float(cond_max):.6e}")
    print(f"prob entropy      = {float(entropy):.6f}")
    print(f"effective N       = {float(eff_n):.2f}")
    print(f"herm_error        = {float(herm_error):.6e}")
    print(f"imag_norm         = {float(imag_norm):.6e}")

    print("E_herm:")
    for i, e in enumerate(np.asarray(eig_herm)):
        print(f"  E{i}_herm = {e: .10f}")

    print("E_raw:")
    for i, e in enumerate(np.asarray(eig_raw)):
        print(f"  E{i}_raw  = {e.real: .10f} {e.imag:+.3e}j")

    return {
        "eig_herm": eig_herm,
        "eig_raw": eig_raw,
        "herm_error": herm_error,
        "imag_norm": imag_norm,
    }
```

---

# 7. 枚举版训练循环

第一版建议：

```python
Natural_Grad = False
lr = 5e-4
clip_norm = 0.5
```

别一上来 `lr=0.01`，那是把干净实验重新污染。之前你 6-31G 训练里已经见过梯度消失、采样塌缩、病态 local energy，这些都需要保守超参先跑通。你的稳定性说明里也写过 6-31G 建议先用更保守的普通梯度，例如 `lr=0.001`、`clip_norm=1.0`，普通梯度稳定后再考虑自然梯度。

```python
# ============================================================
# Exact enumeration NES-VMC training
# ============================================================

N_ITER_ENUM = 500
PRINT_EVERY = 10

lr_enum = 5e-4
clip_norm_enum = 0.5

optimizer_enum = optax.chain(
    optax.clip_by_global_norm(clip_norm_enum),
    optax.sgd(learning_rate=lr_enum),
)

opt_state_enum = optimizer_enum.init(total_params)

history_enum = {
    "step": [],
    "loss": [],
    "grad_norm": [],
    "valid_ratio": [],
    "cond_mean": [],
    "cond_max": [],
    "eff_sample_size": [],
    "prob_entropy": [],
    "E_L_mean": [],
    "eig_herm": [],
    "eig_raw": [],
    "herm_error": [],
    "imag_norm": [],
}

print("=" * 90)
print("Start exact-enumeration NES-VMC training")
print("=" * 90)
print(f"K = {K}")
print(f"valid_X.shape = {valid_X.shape}")
print(f"lr = {lr_enum}")
print(f"clip_norm = {clip_norm_enum}")
print(f"target_loss = {float(np.sum(np.asarray(E_fcis[:K]))):.10f}")
print("=" * 90)

start_time = time.time()

for step in range(N_ITER_ENUM):

    grad_raw, loss_exact, E_L_mean, aux = exact_nes_gradient_covariance(
        total_params=total_params,
        x_all=valid_X,
        ha=ha,
        total_machine=total_machine,
        total_matrix_machine=total_matrix_machine,
        total_max_machine=total_max_machine,
        single_machine_list=single_machine_list,
    )

    grad_flat, _ = ravel_pytree(grad_raw)
    grad_norm_raw = jnp.linalg.norm(grad_flat)

    grad_finite = jnp.all(jnp.isfinite(grad_flat))
    loss_finite = jnp.isfinite(loss_exact)

    if (not bool(grad_finite)) or (not bool(loss_finite)):
        print(f"[Step {step}] STOP: non-finite loss/grad")
        print(f"loss_finite={bool(loss_finite)}, grad_finite={bool(grad_finite)}")
        break

    updates, opt_state_enum = optimizer_enum.update(
        grad_raw,
        opt_state_enum,
        total_params,
    )

    total_params = optax.apply_updates(total_params, updates)

    if step % PRINT_EVERY == 0 or step == N_ITER_ENUM - 1:
        diag_out = exact_enum_diagnostics(
            aux,
            E_fcis=E_fcis,
            K=K,
        )

        history_enum["step"].append(step)
        history_enum["loss"].append(loss_exact)
        history_enum["grad_norm"].append(grad_norm_raw)
        history_enum["valid_ratio"].append(aux["valid_ratio"])
        history_enum["cond_mean"].append(aux["cond_mean"])
        history_enum["cond_max"].append(aux["cond_max"])
        history_enum["eff_sample_size"].append(aux["eff_sample_size"])
        history_enum["prob_entropy"].append(aux["prob_entropy"])
        history_enum["E_L_mean"].append(E_L_mean)
        history_enum["eig_herm"].append(diag_out["eig_herm"])
        history_enum["eig_raw"].append(diag_out["eig_raw"])
        history_enum["herm_error"].append(diag_out["herm_error"])
        history_enum["imag_norm"].append(diag_out["imag_norm"])

end_time = time.time()

print("=" * 90)
print("Exact-enumeration training finished")
print(f"Elapsed time = {end_time - start_time:.2f} sec")
print("=" * 90)
```

---

# 8. 加一个 exact span 诊断

这个不是 NES loss，而是检查三个 single ansatz 张成的子空间到底靠不靠谱。你之前的稳定性说明里也写过，H₂ / 6-31G 单系统只有 16 个 determinant，所以必须做 exact span diagnostics；如果 exact Ritz 对、sampled loss 不对，就是 sampler / local estimator 的问题；如果 exact Ritz 也不对，就是 ansatz / 优化没学到目标子空间。

```python
def exact_span_diagnostics(
    hi,
    ha,
    single_machine_list,
    total_params,
    eps=1e-10,
):
    """
    检查 K 个 single ansatz 张成的线性子空间。

    返回:
        ritz_vals
        S
        overlap
        cond_S
    """
    states = jnp.asarray(hi.all_states())
    H = jnp.asarray(ha.to_dense())

    cols = []

    for j, machine_j in enumerate(single_machine_list):
        params_j = total_params["single_ansatz_list"][j]

        logpsi_j = machine_j(params_j, states)

        # 只做列内数值稳定，不改变张成空间
        logpsi_j = logpsi_j - jnp.max(jnp.real(logpsi_j))

        psi_j = jnp.exp(logpsi_j)
        cols.append(psi_j)

    Phi = jnp.stack(cols, axis=1)  # shape: (N_single, K)

    S = Phi.conj().T @ Phi
    H_eff = Phi.conj().T @ H @ Phi

    K_local = len(single_machine_list)
    S_reg = S + eps * jnp.eye(K_local, dtype=S.dtype)

    ritz_mat = jnp.linalg.solve(S_reg, H_eff)

    ritz_vals = jnp.linalg.eigvals(ritz_mat)
    ritz_vals = ritz_vals[jnp.argsort(jnp.real(ritz_vals))]

    norms = jnp.sqrt(jnp.real(jnp.diag(S)))
    overlap = S / (norms[:, None] * norms[None, :] + eps)

    cond_S = jnp.linalg.cond(S)

    return ritz_vals, S, overlap, cond_S


ritz_vals, S, overlap, cond_S = exact_span_diagnostics(
    hi=hi,
    ha=ha,
    single_machine_list=single_machine_list,
    total_params=total_params,
)

print("=" * 80)
print("Exact span diagnostics")
print("=" * 80)
print("Ritz values:")
for i, e in enumerate(np.asarray(ritz_vals)):
    print(f"E{i}_ritz = {e.real:.10f} {e.imag:+.3e}j")

print("\nFCI target:")
for i in range(K):
    print(f"E{i}_FCI  = {float(E_fcis[i]):.10f}")

print("\ncond(S) =", float(cond_S))
print("\nOverlap matrix:")
print(np.asarray(overlap))
print("=" * 80)
```

---

# 9. 你应该如何判断结果

## 情况 A：枚举版 loss 能稳定下降

说明：

```text
NES loss / E_L matrix / gradient 主链路大概率没问题。
```

那你之前 K=3 失败主要就是：

```text
MCMC sampler collapse
proposal 不够强
learning rate 太大
bad-state 没 rollback
```

## 情况 B：枚举版也很快 NaN / cond 爆炸

说明 sampler 不是主锅。问题在：

```text
NESTotalAnsatz 的 K 列线性相关
E_L matrix solve 病态
Jastrow amplitude drift
optimizer 过猛
gauge fixing 不够
```

## 情况 C：loss 下降，但 exact Ritz 不靠近 FCI 三态

说明：

```text
NES loss 在优化，但 K 个 single ansatz 没有正确张成低能三维子空间。
```

这时候要做：

```text
FCI / CISD pretraining
orthogonalization penalty
state overlap diagnostics
```

## 情况 D：exact span Ritz 接近 FCI，但 NES loss 不对

说明：

```text
single ansatz 张成空间其实对了，但 NES determinant / local E_L estimator 或权重公式有问题。
```

这就要回头查：

```text
total_machine
total_matrix_machine
Ham_Psi_scaled
E_L solve
prob weights
```

---

# 10. 最后提醒一个坑

你之前有一处旧代码把：

```python
psi_mat = total_matrix_machine(total_params, x_single)[0]
psi_cond = jnp.linalg.cond(psi_mat)
```

当成 (\Psi) 的条件数。这个是错的。`total_matrix_machine` 返回的是 `L_stable`，真正应该算：

```python
L_stable_single = total_matrix_machine(total_params, x_single)[0]
Psi_stable_single = jnp.exp(L_stable_single)
psi_cond = jnp.linalg.cond(Psi_stable_single)
```

你稳定性说明里已经明确记录过这个坑。
我上面给你的枚举代码已经按正确版本算的是 `cond(Psi_stable)`。

---

你先跑这版。第一眼看这几个数：

```text
Loss exact
Grad norm
valid_ratio
cond(Psi) max
effective N
E0/E1/E2 herm
cond(S)
overlap matrix
```

尤其是 `cond(S)` 和 `overlap matrix`。如果三个 single ansatz 又挤成一坨，那 K=3 不稳定就不是 sampler 的锅了，而是模型在搞“三个态穿同一件衣服”的抽象艺术。
